import 'package:flutter/material.dart';

/// A central repository for all color constants used in the application.
class AppColors {
  /// Main brand color.
  static const Color primary = Colors.blue;
  
  /// A lighter shade of the primary color.
  static const Color primaryLight = Colors.lightBlue;
  
  /// Primary color for text elements.
  static const Color textPrimary = Colors.black;
  
  /// Specific background color used for brown-themed buttons.
  /// TODO: Consider renaming this to a more semantic name (e.g., secondaryActionBackground).
  static const Color backgroundButtonBrown = Colors.brown;
}
